apiVersion: 1

datasources:
  
  - name: MySQL
    type: mysql
    access: proxy
    url: mysql:3306
    database: simulation_db
    user: user
    secureJsonData:
      password: userpassword
    isDefault: true
    editable: true